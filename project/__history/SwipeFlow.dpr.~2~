program SwipeFlow;

uses
  System.StartUpCopy,
  FMX.Forms,
  Main in 'Main.pas' {Form3},
  Core in 'Core.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm3, Form3);
  Application.Run;
end.
